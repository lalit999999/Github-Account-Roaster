==> High Priority Issues
1. Duplicate Validation Logic
Username validation exists in both UsernameInput.tsx and App.tsx
Fix: Extract to a shared utility (src/app/utils/validation.ts)
2. Poor Type Safety
(import.meta as any).env.VITE_API_URL loses type safety
Fix: Use proper vite.config.ts typing and create a constants file
3. No Response Schema Validation
API responses are used without validation
Fix: Add Zod/Yup schemas for API responses
4. Error Mapping is Fragile
Manual string mapping between backend and frontend errors (line 100-110 in App.tsx)
Fix: Share error types between backend and frontend (or use generated types from API)
🟡 Medium Priority Issues
5. State Management Could Be Cleaner
Three separate useState calls for related state (isLoading, roastData, error)
Fix: Use useReducer for better state cohesion
6. No Request Timeout Handling
fetch() can hang indefinitely
Fix: Add AbortController with timeout
7. Avatar Fallback Missing
If GitHub avatar fails to load, user sees broken image
Fix: Add fallback to initials or placeholder
8. Magic Strings Scattered
Regex patterns, API paths, URLs hardcoded in components
Fix: Create src/app/config.ts for centralized config
🟢 Lower Priority (Polish)
9. Accessibility Gaps
Missing ARIA labels on form inputs
Animation might cause motion sickness (no reduced-motion support)
Fix: Add aria-labels, check for prefers-reduced-motion
10. No Error Recovery
Users can't retry failed requests easily
Fix: Add retry button in error state
11. Weak Loading UX
Random loading message on every render (should be deterministic per request)
Fix: Set message once at start of request
Would you like me to implement fixes for any of these? I'd recommend starting with #1-4 (validation, types, API validation) as they directly impact reliability and maintainability.
